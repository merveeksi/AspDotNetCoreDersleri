<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/lib/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/lib/bootstrap-icons/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <title>Document</title>
</head>
<body>
<nav class="navbar navbar-expand-lg bg-white shadow-sm">
    <div class="container">
        <a href="/posts" class="navbar-brand">
            <i class="bi bi-code"></i> BlogApp
        </a>
        
        <ul class="navbar-nav ms-auto">
            @if(User.Identity!.IsAuthenticated) @* Kullanıcı giriş yapmışsa *@ @*not null assertion*@
            {
                <li class="nav-item">
                    <a href="#" class="nav-link">@User.Identity.Name</a> @* Kullanıcı adını yazdırıyoruz *@
                </li>
                <li class="nav-item">
                    <a href="/users/logout" class="nav-link">Logout</a> @*uygulamadan çıkış linki*@
                </li>
            }
            else @* Kullanıcı giriş yapmamışsa *@
            {
                <li class="nav-item">
                    <a href="/users/login" class="nav-link">Login</a> @* uygulamaya giriş linki *@
                </li>
                <li class="nav-item">
                    <a href="/users/register" class="nav-link">Register</a> @* uygulamaya kayıt linki *@
                </li>
            }
            
        </ul>
    </div>
</nav>
<div class="container mt-3">
    @RenderBody()
</div>
@RenderSection("Scripts", required: false) @* required: false ile bu bölümün zorunlu olmadığını belirtiyoruz *@
</body>
</html>